<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.board.mapper.BoardMapper">

	<select id="findAll" resultType="com.example.board.dto.BoardDTO">
		SELECT * FROM tb_board
		<where>
			<if test="keyword != null and keyword != ''">
				<choose>
					<when test="searchType == 'title'">
						AND title LIKE CONCAT('%', #{keyword}, '%')
					</when>
					<when test="searchType == 'content'">
						AND content LIKE CONCAT('%', #{keyword}, '%')
					</when>
					<when test="searchType == 'writer'">
						AND writer LIKE CONCAT('%', #{keyword}, '%')
					</when>
				</choose>
			</if>
		</where>

		<choose>
			<when test= "sortType == 'latest'">
				ORDER  BY created_at DESC, idx DESC
			</when>

			<when test = "sortType == 'oldest'">
				ORDER BY created_at ASC, idx ASC
			</when>

			<when test = "sortType == 'viewDesc'">
				ORDER BY view_cnt DESC, created_at DESC, idx DESC
			</when>

			<when test = "sortType == 'viewAsc'">
				ORDER BY view_cnt ASC, created_at DESC, idx DESC
			</when>

			<otherwise>
				ORDER BY created_at DESC, idx DESC
			</otherwise>
		</choose>

		LIMIT #{size} OFFSET #{offset}
	</select>

	<select id="countLike" resultType="int">
		SELECT COUNT(*) FROM tb_board_like
			WHERE board_idx = #{idx}
	</select>

	<select id="existsLike" resultType="int">
		SELECT COUNT(*) FROM tb_board_like
			WHERE board_idx = #{idx}
				AND user_id = #{userId}
	</select>
	
	<select id="insertLike">
		INSERT INTO tb_board_like  (board_idx, user_id)
		VALUES (#{idx}, #{userId})
	</select>
	
	<select id="deleteLike">
		DELETE	FROM tb_board_like 
			WHERE board_idx = #{idx}
				AND user_id = #{userId}
	</select>

	<insert id="save" parameterType="com.example.board.dto.BoardDTO">
			INSERT INTO tb_board (title, content, writer, user_id,  board_pw, view_cnt, created_at, updated_at)
			Values (#{title}, #{content}, #{writer}, #{userId}, #{boardPw}, 0, NOW(), NOW())
	</insert>


	<select id="findById" parameterType="Long" resultType="com.example.board.dto.BoardDTO">
		SELECT * FROM tb_board WHERE idx = #{idx}
	</select>

	<update id="update" parameterType="com.example.board.dto.BoardDTO">
		UPDATE tb_board
		SET title = #{title},
			content = #{content},
			updated_at = NOW()
		where idx = #{idx}
	</update>

	<delete id="delete" parameterType="Long">
		DELETE FROM tb_board WHERE idx = #{idx}
	</delete>

	<update id="updateViewCnt" parameterType="Long">
		UPDATE tb_board
		SET view_cnt = view_cnt + 1
		WHERE idx = #{idx}
	</update>

	<select id="count" resultType="int">
		SELECT count(*) FROM tb_board
		<where>
			<if test="keyword != null and keyword != ''">
				<choose>
					<when test="searchType == 'title'">
						AND title LIKE CONCAT('%', #{keyword}, '%')
					</when>
					<when test="searchType == 'content'">
						AND content LIKE CONCAT('%', #{keyword}, '%')
					</when>
					<when test="searchType == 'writer'">
						AND writer LIKE CONCAT('%', #{keyword}, '%')
					</when>
				</choose>
			</if>
		</where>
	</select>
</mapper>